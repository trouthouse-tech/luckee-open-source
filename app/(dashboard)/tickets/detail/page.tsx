'use client';

import { TicketDetailPage } from '@/src/packages/ticket-detail-page';

/**
 * Ticket Detail Page Route
 * Renders the ticket-detail-page package (inline edit, no modal).
 */
export default function TicketDetailRoute() {
  return <TicketDetailPage />;
}
